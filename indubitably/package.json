{
  "name": "@shellmark/indubitably",
  "version": "0.2.0",
  "private": true,
  "description": "Phase 0 service and protocol specifications for verifiable agent-to-agent work.",
  "type": "module",
  "engines": {
    "node": ">=22.16"
  },
  "scripts": {
    "start": "node src/main.mjs",
    "test": "node --test test/*.test.mjs",
    "validate": "npm run validate:schemas && npm run validate:docs && npm run validate:plugin && npm test",
    "validate:schemas": "node scripts/validate-schemas.mjs",
    "validate:docs": "node scripts/validate-docs.mjs",
    "keygen": "node scripts/generate-agent-key.mjs",
    "validate:plugin": "node scripts/validate-openclaw-plugin.mjs"
  }
}
